\begin{figure}
\begin{mathpar}
\inferrule[Atom Sumstar]
{
A\subsetneq \RangeIncInc{1}{n}
}
{
\RegexContext \DerivesAtomRewrite \Star{([\Conjunct_1;\ldots;\Conjunct_n])}\RewriteAtom\\
\ConcatDNF(\Star{(\ConcatDNF([[\EmptyString;\Star{\DNFRegex_A};\EmptyString]],\DNFRegex_{\bar{A}}))},
\Star{\DNFRegex_A})\\
\text{where }\DNFRegex_S=[\Conjunct_{S_1};\ldots;\Conjunct_{S_{\SizeOf{S}}}]\\
}

\inferrule[Atom Powerstar]
{
n\in\Nats_{\geq1}
}
{
\RegexContext \DerivesAtomRewrite \Star{(\DNFRegex)}\RewriteAtom\\
\ConcatDNF([[\EmptyString;\Star{[[\RepeatDNF(n,\DNFRegex)]]};\EmptyString]],\StarModNDNF(n,\DNFRegex))
}

\inferrule[$\delta$ Expansion]
{
}
{
\RegexContext'\cup(\RegexVariable,\Regex) \DerivesAtomRewrite \RegexVariable\RewriteAtom
\DNFRegex_{\Regex}\\
\text{where $\DNFRegex_{\Regex}$ is a DNF Regex such that $\LanguageOf{\RegexContext'}{\Regex}=\LanguageOf{\RegexContext'}{\DNFRegex_{\Regex}}$}.
}

\inferrule[DNF Rewrite Star]
{
\RegexContext \DerivesDNFRewrite \DNFRegex \RewriteDNF \DNFRegex'
}
{
\RegexContext \DerivesAtomRewrite \Star{\DNFRegex} \RewriteAtom [[\EmptyString;\Star{\DNFRegex'};\EmptyString]]
}

\inferrule[Atom DNF Rewrite]
{
\RegexContext \DerivesAtomRewrite \Atom_j \RewriteAtom \DNFRegex
}
{
\RegexContext \DerivesDNFRewrite [\Clause_1;\ldots;\Clause_{i-1};\\\\
[\String_0;\Atom_1;\ldots;\String_{j-1};\Atom_j;\String_j;\ldots;\Atom_m;\String_m]\\\\
;\Clause_{i+1};\ldots;\Clause_n]\RewriteDNF\\\\
\OrDNF(\OrDNF([\Clause_1;\ldots;\Clause_{i-1}],\\
\ConcatDNF(\ConcatDNF([[\String_0;\Atom_1;\ldots;\String_{j-1}]],\DNFRegex),[\String_j;\ldots;\Atom_m;\String_m])),\\
[\Clause_{i+1};\ldots;\Clause_n])
}

\inferrule[DNF Rewrite Composition]
{
\RegexContext \DerivesDNFRewrite \DNFRegex \RewriteDNF \DNFRegex'\\
\RegexContext \DerivesDNFRewrite \DNFRegex' \RewriteDNF \DNFRegex''
}
{
\RegexContext \DerivesDNFRewrite \DNFRegex \RewriteDNF \DNFRegex''
}

\inferrule[Identity DNF Rewrite]
{
}
{
\RegexContext \DerivesDNFRewrite \DNFRegex \RewriteDNF \DNFRegex
}

\end{mathpar}
\caption{DNF Regex Rewrite Rules}
\label{fig:dnf-regex-rewrites}
\end{figure}
